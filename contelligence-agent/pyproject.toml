[project]
name = "contelligence-agent"
version = "0.1.3"
description = "Contelligence Agent — Intelligent content processing."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
filterwarnings = [
    "ignore::DeprecationWarning",
]
